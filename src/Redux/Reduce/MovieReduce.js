const initialState = {
    mangND: [
        {
            name: "",
            number:"",
            seats:"",
        }
    ]
}

export const MovieReduce = (state = initialState, action) => {
    switch (action.type) {


        default:
            return state
    }
}
